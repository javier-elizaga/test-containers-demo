application:
  test-containers:
    oracle-image: larmic/oracle-xe:18.4.0

spring:
  jpa:
    hibernate:
      ddl-auto: update
      use-new-id-generator-mappings: false
    database-platform: org.hibernate.dialect.Oracle12cDialect